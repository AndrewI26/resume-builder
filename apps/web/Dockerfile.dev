FROM node:22-slim

RUN npm install -g bun

WORKDIR /app

COPY package.json bun.lock bunfig.toml ./
COPY apps/web/package.json apps/web/package.json
RUN bun install

COPY . .

EXPOSE 5173

CMD ["sh", "-c", "bun install && bun run --filter apps-web dev -- --host 0.0.0.0 --port ${PORT:-5173}"]
