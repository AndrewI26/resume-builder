{
  "name": "resume-builder",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "bun run --filter apps-web dev",
    "build:web": "bun run --filter apps-web build",
    "codegen": "bun run --filter apps-web generate-api",
    "docker:dev": "docker compose -f docker-compose.dev.yml up",
    "docker:dev:down": "docker compose -f docker-compose.dev.yml down",
    "db:revision": "uv run --directory apps/api alembic revision --autogenerate -m",
    "db:upgrade": "uv run --directory apps/api alembic upgrade head",
    "db:seed": "uv run --directory apps/api python -m scripts.seed",
    "db:backup": "uv run --directory apps/api python -m scripts.backup_db",
    "dev:api": "uv run --directory apps/api uvicorn main:app --reload",
    "format:web": "bun run --filter apps-web format",
    "lint:web": "bun run --filter apps-web lint",
    "typecheck:web": "bun run --filter apps-web typecheck",
    "format:api": "uv run --directory apps/api ruff format .",
    "lint:api": "uv run --directory apps/api ruff check --fix .",
    "typecheck:api": "uv run --directory apps/api mypy .",
    "test:api": "uv run --directory apps/api pytest",
    "test:web": "bun run --filter apps-web test",
    "test": "bun run test:api && bun run test:web",
    "verify": "bun run format:web && bun run lint:web && bun run typecheck:web && bun run format:api && bun run lint:api && bun run typecheck:api",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@types/bun": "latest",
    "apps-web": "workspace:*",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "volta": {
    "node": "24.19.0"
  }
}
